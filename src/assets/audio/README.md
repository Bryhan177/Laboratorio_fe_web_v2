# Carpeta de Audio

Coloca aquí tu archivo de audio de fondo con el nombre:
`background.mp3`

Formatos recomendados:
- `.mp3` (compatible con todos los navegadores)
- `.ogg` o `.m4a`

La ruta configurada en el componente es:
`assets/audio/background.mp3`
